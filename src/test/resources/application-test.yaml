spring:
  flyway:
    enabled: true
  data:
    redis:
      host: localhost
      port: 6379

app:
  redis:
    channel:
      discount-eviction: discount-eviction-topic

eureka:
  client:
    enabled: false
